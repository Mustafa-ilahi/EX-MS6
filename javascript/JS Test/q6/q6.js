document.write("<h1>" + "Question 6" + "</h1>")
for (var i = 0; i < 4; i++) {
    setTimeout(() => console.log(i), 0)
    }
    // the for loop executes first, then it looks for the i value, which is 4 nd then outputs four times, one for each loop iteration    